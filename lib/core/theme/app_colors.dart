import 'package:flutter/material.dart';

/// Paleta de colores de Cloud API CC.
class AppColors {
  AppColors._();

  // Marca
  static const Color brand = Color(0xFF004479);
  static const Color brandDark = Color(0xFF00305A);
  static const Color brandAccent = Color(0xFFFFCC00);
  static const Color brandAccentForeground = Color(0xFF004479);

  // Superficies
  static const Color canvas = Color(0xFFEEF1F6);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color inputBackground = Color(0xFFF4F6F9);
  static const Color chatBackground = Color(0xFFFAFBFD);

  // Texto
  static const Color foreground = Color(0xFF1A1A1A);
  static const Color mutedForeground = Color(0xFF717182);

  // Estados
  static const Color destructive = Color(0xFFD4183D);
  static const Color destructiveForeground = Color(0xFFFFFFFF);
  static const Color success = Color(0xFF059669);

  // Bordes
  static const Color border = Color(0x1A000000);
  static const Color borderLight = Color(0x0D000000);
}
